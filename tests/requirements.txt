# Dépendances de test centralisées — utilisées par la CI et le dev local.
# Toutes les deps RUNTIME des services sont déjà tirées via leurs propres
# requirements.txt ; ici on ajoute uniquement ce qui sert aux tests.

# Test runner + couverture
pytest>=8.0
pytest-cov>=5.0

# fastapi.testclient s'appuie sur starlette.testclient qui requiert httpx
# depuis Starlette 0.37+. Ce paquet n'est plus tiré automatiquement.
httpx>=0.27

# Outils qualité (alignés avec la matrice de lint)
flake8>=7.0
black>=24.0
