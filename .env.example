# =============================================================================
# MECHA — Solution IA Maintenance Prédictive
# Copier en .env et adapter aux valeurs locales (jamais versionner le .env).
# =============================================================================

# -----------------------------------------------------------------------------
# Pipeline Bronze → Silver → Gold
# -----------------------------------------------------------------------------
BRONZE_DIR=/data/bronze
SILVER_DIR=/data/silver
GOLD_DIR=/data/gold
MODELS_DIR=/models
LOGS_DIR=/logs

# -----------------------------------------------------------------------------
# Logique métier — seuils et planification
# -----------------------------------------------------------------------------
FAILURE_ALERT_THRESHOLD=0.75
ETL_CRON_SCHEDULE=0 */6 * * *      # toutes les 6 heures
RETRAIN_CRON_SCHEDULE=0 2 * * 0    # chaque dimanche 02h00 UTC

# -----------------------------------------------------------------------------
# Observabilité
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT=json

# -----------------------------------------------------------------------------
# API FastAPI — interne au réseau mecha-net
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=2
API_BASE_URL=http://api:8000

# -----------------------------------------------------------------------------
# MLflow (tracking des expériences trainer)
# -----------------------------------------------------------------------------
MLFLOW_TRACKING_URI=file:///models/mlruns
MLFLOW_EXPERIMENT_NAME=mecha-failure-prediction

# -----------------------------------------------------------------------------
# Postgres — datastore opérationnel pour Grafana
# -----------------------------------------------------------------------------
POSTGRES_USER=mecha
POSTGRES_PASSWORD=mecha_change_me
POSTGRES_DB=mecha
POSTGRES_PORT=5432

# DSN utilisé par l'ETL et l'API pour se connecter à Postgres
POSTGRES_DSN=postgresql+psycopg2://mecha:mecha_change_me@postgres:5432/mecha

# -----------------------------------------------------------------------------
# Grafana — seul service exposé au host
# -----------------------------------------------------------------------------
GRAFANA_PORT=3000
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=mecha_change_me

# -----------------------------------------------------------------------------
# Prometheus — scraping des métriques de l'API
# -----------------------------------------------------------------------------
PROMETHEUS_PORT=9090

# -----------------------------------------------------------------------------
# Mode DEMO — rejeu temps réel du jeu de données (profil compose `demo`)
# DEMO_SOURCE : auto (Gold si présent, sinon synthétique) | gold | synthetic
# -----------------------------------------------------------------------------
DEMO_SOURCE=auto
DEMO_INTERVAL_SECONDS=2
DEMO_MACHINES=5
DEMO_SYNTHETIC_POINTS=720
DEMO_LOOP=true

# -----------------------------------------------------------------------------
# Identité applicative (à surcharger par environnement)
# -----------------------------------------------------------------------------
APP_ENV=development
APP_SITE_ID=local
